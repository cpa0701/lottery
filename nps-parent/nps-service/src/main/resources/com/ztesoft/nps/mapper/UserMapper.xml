<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ztesoft.nps.mapper.UserMapper" >
  <resultMap id="BaseResultMap" type="User" >
    <id column="id" property="id" />
    <result column="no" property="no" />
    <result column="account" property="account" />
    <result column="name" property="name" />
    <result column="password" property="password" />
    <result column="salt" property="salt" />
    <result column="dept_id" property="deptId" />
    <result column="created_by" property="createdBy" />
    <result column="created_at" property="createdAt" />
    <result column="modified_by" property="modifiedBy" />
    <result column="modified_at" property="modifiedAt" />
  </resultMap>
  
  <sql id="Base_Column_List" >
    id, no, account, name, password, salt, dept_id, created_by, created_at, modified_by, modified_at
  </sql>
  
  <select id="findById" resultMap="BaseResultMap" parameterType="Long" >
    select 
    <include refid="Base_Column_List" />
    from users
    where id = #{id}
  </select>
  
  <select id="findByDeptId" resultMap="BaseResultMap" parameterType="Long" >
    select 
    <include refid="Base_Column_List" />
    from users
    where dept_id = #{deptId}
  </select>
  
  <insert id="add" parameterType="User" useGeneratedKeys="true" keyProperty="id">
    insert into users (no, account, name, password, salt, dept_id, created_by, created_at, modified_by, modified_at)
    values (#{no}, #{account}, #{name}, #{password}, #{salt}, #{deptId}, #{createdBy}, #{createdAt}, #{modifiedBy}, #{modifiedAt})
  </insert>
  
  <select id="findByAccount" resultMap="BaseResultMap" parameterType="String" >
    select 
    <include refid="Base_Column_List" />
    from users
    where account = #{account}
  </select>
  
  <select id="findByNo" resultMap="BaseResultMap" parameterType="String" >
    select 
    <include refid="Base_Column_List" />
    from users
    where no = #{no}
  </select>

</mapper>